kind: AppProject
apiVersion: argoproj.io/v1alpha1
metadata:
  name: online-app
  namespace: argocd
spec:
  description: Project space for Online Web-Store Application
  sourceRepos:
    - '*'
  destinations:
    - namespace: application # match it with the namespace in the main.yml
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: ''
      kind: application #for principle of least privilage replace it with the application namespace